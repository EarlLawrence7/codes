#include<stdio.h>
int main() {

float diameter, area1=0, area2=0;

printf("\nEnter the diameter of Circle :");
scanf("%f", &diameter);

area1=2*radius;
area2=2*radius;

printf("\nArea of Circle : %f", area);
return (0);
